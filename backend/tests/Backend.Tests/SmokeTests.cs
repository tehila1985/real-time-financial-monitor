namespace Backend.Tests;

/// <summary>
/// Placeholder confirming the test project is wired up (references Backend, runs under xUnit).
/// Replaced by real Storage/Service tests in Phase 1 (see docs/DESIGN.md §22).
/// </summary>
public class SmokeTests
{
    [Fact]
    public void TestHarness_IsWiredUp()
    {
        Assert.True(true);
    }
}
